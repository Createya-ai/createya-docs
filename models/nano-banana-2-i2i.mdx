---
title: 2
description: 2. Генерация через Createya API — без VPN, оплата в рублях. AI генерация изображений, AI генерация видео, без VPN, в рублях.
sidebar:
  label: 2
---

# 2

> 2

**Стоимость:** 10 кредитов / запрос (resolution: 0.5K=10кр, 1K=10кр, 2K=15кр, 4K=20кр; × num_images (1–4); +2кр с enable_web_search).

**Family slug:** `nano` · **Output:** image · **Mode:** sync



## Быстрый старт

### curl

```bash
curl -X POST https://api.createya.ai/v1/run \
  -H "Authorization: Bearer $CREATEYA_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "nano-banana-2-i2i",
    "input": {
      "prompt": "your prompt"
    }
  }'
```

### Python

```python
import os, requests
KEY = os.environ["CREATEYA_API_KEY"]
r = requests.post(
    "https://api.createya.ai/v1/run",
    headers={"Authorization": f"Bearer {KEY}"},
    json={"model": "nano-banana-2-i2i", "input": {"prompt": "your prompt"}}
)
print(r.json())
```

### Node.js

```javascript
const r = await fetch("https://api.createya.ai/v1/run", {
  method: "POST",
  headers: {
    "Authorization": `Bearer ${process.env.CREATEYA_API_KEY}`,
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    model: "nano-banana-2-i2i",
    input: { prompt: "your prompt" }
  })
});
console.log(await r.json());
```

## Параметры

| Параметр | Тип | Required | Описание |
|---|---|---|---|
| `num_images` | integer |  | Количество генерируемых изображений. |
| `enable_web_search` | boolean |  | Включить веб-поиск для задачи генерации изображений. Это позволит модели использовать актуальную информацию из сети для создания изображения. |
| `resolution` | enum |  | Разрешение генерируемого изображения. |
| `aspect_ratio` | enum |  | Соотношение сторон генерируемого изображения. Поддерживаются экстремальные значения: 4:1, 1:4, 8:1, 1:8. |
| `output_format` | enum |  | Формат генерируемого изображения. |
| `thinking_level` | enum |  | При установке включает процесс размышления модели с заданным уровнем («minimal» или «high») и добавляет ход мыслей в результат. Оставьте пустым, чтобы отключить. |
| `safety_tolerance` | enum |  | Уровень строгости модерации контента. 1 — самый строгий (блокирует большую часть контента), 6 — наименее строгий. |
| `seed` | integer |  | Сид (seed) для генератора случайных чисел. |

## Похожие модели

См. [полный каталог](/models/) или [createya.ai/knowledge](https://createya.ai/knowledge).

## Через MCP

```
createya:run_model({
  model: "nano-banana-2-i2i",
  input: { prompt: "your prompt" }
})
```

См. [MCP Integration](/mcp/overview/) для подключения к Claude / Cursor / Cline.
