---
title: Pro
description: Pro. Генерация через Createya API — без VPN, оплата в рублях. AI генерация изображений, AI генерация видео, без VPN, в рублях.
sidebar:
  label: Pro
---

# Pro

> Pro

**Стоимость:** 18 кредитов / запрос (resolution: 1K=18кр, 2K=18кр, 4K=36кр; × num_images (1–4); +2кр с enable_web_search).

**Family slug:** `nano` · **Output:** image · **Mode:** sync



## Быстрый старт

### curl

```bash
curl -X POST https://api.createya.ai/v1/run \
  -H "Authorization: Bearer $CREATEYA_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "nano-banana-pro-i2i",
    "input": {
      "prompt": "your prompt"
    }
  }'
```

### Python

```python
import os, requests
KEY = os.environ["CREATEYA_API_KEY"]
r = requests.post(
    "https://api.createya.ai/v1/run",
    headers={"Authorization": f"Bearer {KEY}"},
    json={"model": "nano-banana-pro-i2i", "input": {"prompt": "your prompt"}}
)
print(r.json())
```

### Node.js

```javascript
const r = await fetch("https://api.createya.ai/v1/run", {
  method: "POST",
  headers: {
    "Authorization": `Bearer ${process.env.CREATEYA_API_KEY}`,
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    model: "nano-banana-pro-i2i",
    input: { prompt: "your prompt" }
  })
});
console.log(await r.json());
```

## Параметры

| Параметр | Тип | Required | Описание |
|---|---|---|---|
| `resolution` | enum |  | Разрешение генерируемого изображения. |
| `enable_web_search` | boolean |  | Включить веб-поиск для задачи генерации изображений. Это позволит модели использовать актуальную информацию из сети для создания изображения. |
| `num_images` | integer |  | Количество генерируемых изображений. |
| `aspect_ratio` | enum |  | Соотношение сторон генерируемого изображения. |
| `output_format` | enum |  | Формат генерируемого изображения. |
| `safety_tolerance` | enum |  | Уровень допустимой безопасности для модерации контента. 1 — самый строгий (блокирует большую часть контента), 6 — наименее строгий. |
| `seed` | integer |  | Сид (seed) для генератора случайных чисел. |
| `limit_generations` | boolean |  | Экспериментальный параметр для ограничения количества генераций до 1 за один цикл запроса. Установите значение `True`, чтобы игнорировать любые инструкции в промпте, касающиеся количества создаваемых изображений. |

## Похожие модели

См. [полный каталог](/models/) или [createya.ai/knowledge](https://createya.ai/knowledge).

## Через MCP

```
createya:run_model({
  model: "nano-banana-pro-i2i",
  input: { prompt: "your prompt" }
})
```

См. [MCP Integration](/mcp/overview/) для подключения к Claude / Cursor / Cline.
